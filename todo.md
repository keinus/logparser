# 성능 최적화 및 아키텍처 개선 항목 (TODO)

프로젝트 아키텍처 및 코드 검토 결과, 성능 병목 현상이 발생할 수 있는 주요 지점과 해결 방안을 정리하였습니다.

## 1. 아키텍처 및 파이프라인 병목 (Architectural Bottlenecks)

### 1.1. 출력 어댑터 팬아웃(Fan-out) 병목
- **문제점**: `OutputAdapterComponent`에서 단일 스레드(`OutputDispatcher`)가 큐의 메시지를 하나씩 꺼내 모든 등록된 어댑터에 순차적으로 전달합니다. 특정 어댑터(예: 느린 HTTP 엔드포인트)에서 지연이 발생하면 전체 출력 파이프라인이 멈추는 현상이 발생합니다.
- **해결 방안**: 
  - 각 어댑터별로 독립적인 큐 또는 워커 스레드 풀을 할당하여 병렬로 메시지를 전송하도록 개선.
  - Project Reactor나 Java 21의 가상 스레드(Virtual Threads)를 활용한 비동기 전송 모델 도입 고려.

### 1.2. 고정된 스레딩 모델
- **문제점**: 파서 및 변환 워커 스레드 수가 설정에 고정되어 있어, 갑작스러운 트래픽 폭증(Burst) 시 큐가 포화되고 인입(Ingestion) 단계에 백프레셔(Backpressure)가 발생하여 전체 시스템이 정지될 수 있습니다.
- **해결 방안**: 
  - 큐의 길이에 따라 동적으로 워커 스레드 수를 조절하는 동적 스케일링 로직 도입.
  - 시스템 리소스(CPU, Memory) 상태를 모니터링하여 임계치 초과 시 유입량을 제어하는 서킷 브레이커 로직 강화.

## 2. 코드 효율성 및 지연 시간 (Code Efficiency & Latency)

### 2.1. 비효율적인 워커 루프 (Poll + Sleep) - [DONE]
- **문제점**: `ParseDispatcher`, `TransformDispatcher` 등에서 큐가 비어있을 때 `poll()` 이후 `ThreadUtil.sleep(100)`을 호출합니다. 이는 새로운 데이터가 들어왔을 때 최대 100ms의 불필요한 지연을 발생시키며, 빈번한 컨텍스트 스위칭을 유발합니다.
- **해결 방안**: 
  - `poll()` + `sleep()` 대신 블로킹 방식인 `take()`를 사용하여 데이터가 들어오는 즉시 처리하고, 데이터가 없을 때는 스레드가 효율적으로 대기하도록 수정. (완료)

### 2.2. 중복 직렬화 (Serialization Overhead)
- **문제점**: `OutputAdapterComponent`에서 동일한 포맷을 사용하는 여러 어댑터가 있을 때, 각 어댑터마다 `LogEvent`를 JSON 등으로 중복하여 직렬화합니다.
- **해결 방안**: 
  - 동일한 출력 포맷을 사용하는 경우, 메시지당 한 번만 직렬화하여 캐싱된 값을 사용하도록 최적화.

## 3. I/O 및 네트워킹 성능 (I/O Performance)

### 3.1. 동기식 및 블로킹 I/O
- **문제점**: `HttpOutputAdapter` 등에서 로우 소켓(Raw Socket)을 사용하거나 `synchronized` 키워드를 사용하여 동기적으로 I/O를 처리합니다. 이는 네트워크 지연 시 호출 스레드를 장시간 점유하게 만듭니다.
- **해결 방안**: 
  - Java 11의 `HttpClient` 또는 Apache HttpComponents 5 (Async) 등 비동기 I/O 라이브러리를 사용하여 논블로킹 방식으로 전환.
  - 과도한 `synchronized` 블록을 제거하고 `ReentrantLock`이나 동시성 컬렉션 활용.

### 3.2. 배치 처리 최적화 미흡
- **문제점**: 일부 어댑터는 로그를 건당으로 처리하여 네트워크 오버헤드가 큽니다.
- **해결 방안**: 
  - `BatchingOutputService`를 적극 활용하여 일정 수량 또는 일정 시간 단위로 로그를 묶어서 전송하는 배치 처리 로직을 모든 출력 어댑터에 공통 적용.

## 4. 메모리 및 리소스 관리

### 4.1. 정규표현식 컴파일 오버헤드
- **문제점**: 파싱 로직에서 정규표현식이 빈번하게 사용될 경우, 매번 컴파일하거나 부적절하게 관리되면 CPU 부하가 커질 수 있습니다.
- **해결 방안**: 
  - `PatternCache` 등을 통해 이미 컴파일된 `Pattern` 객체를 재사용하고 있는지 재점검.
  - Grok 패턴의 경우 미리 컴파일된 상태로 유지.

---
**작성일**: 2025-12-28
**상태**: 검토 필요
